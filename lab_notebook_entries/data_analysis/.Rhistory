str(leaf)
plot(leaf_mass ~ leaf_mass_AFDM)
leaf_mass - leaf_mass_AFDM
data.frame(leaf, leaf_mass-leaf_mass_AFDM)
str(leaf)
plot(AFDM ~ days, data = leaf, subset = lake == "Daulton Pond")
plot(AFDM ~ days, data = leaf, subset = lake == "Daulton Pond", xlim = c(0, 30))
points(AFDM ~ days, data = leaf, subset = lake == "Campus Pond", col = 2)
points(AFDM ~ days, data = leaf, subset = lake == "Lancer Park Pond", col = 3)
q()
n
?write.table
str(leaf)
plot(AFDM ~ leaf_mass)
plot(leaf_mass)
points(AFDM, col = 3)
AFDM
leaf_mass
data.frame(leaf$leaf_mass, leaf$AFDM)
data.frame(leaf$leaf_mass, leaf$AFDM, leaf$leaf_mass - leaf$AFDM)
rm(list = ls())
ls()
rm(list = ls())
str(leaf)
plot(AFDM ~ leaf_mass, data = leaf)
plot(leaf_mass ~ days, data = leaf)
points(AFDM ~ days, data = leaf, pch = 16, col = 2)
    inital.DP <- mean(leaf$AFDM[leaf$lake == "Daulton Pond" & leaf$days == 0])
inital.DP
leaf$AFDM[leaf$lake == "Daulton Pond" & leaf$days == 0])
leaf$AFDM[leaf$lake == "Daulton Pond" & leaf$days == 0]
inital.DP
perc.remaining.DP
    perc.rem.DP <- leaf$AFDM[leaf$lake == "Daulton Pond"] / (T0.DP - leaf$AFDM[leaf$lake == "Daulton Pond"])
perc.rem.DP
T0.DP - leaf.AFDM[leaf$lake == "Daulton Pond"]
T0.DP - leaf$AFDM[leaf$lake == "Daulton Pond"]
(T0.DP - leaf$AFDM[leaf$lake == "Daulton Pond"]) / leaf$AFDM[leaf$lake == "Daulton Pond"]
(T0.DP - leaf$AFDM[leaf$lake == "Daulton Pond"]) / leaf$AFDM[leaf$lake == "Daulton Pond"] * 100
(T0.DP - leaf$AFDM[leaf$lake == "Daulton Pond"]) / leaf$AFDM[leaf$lake == "Daulton Pond"] * 100
    prop.rem.DP <- (T0.DP - leaf$AFDM[leaf$lake == "Daulton Pond"]) / leaf$AFDM[leaf$lake == "Daulton Pond"]
plot(prop.rem.DP ~ days, data = leaf, subset = lake == "Daulton Pond")
prop.rem.DP
days[leaf$lake == "Daulton Pond"]
leaf$days[leaf$lake == "Daulton Pond"]
length(prop.rem.DP)
length(leaf$days[leaf$lake == "Daulton Pond"])
plot(prop.rem.DP ~ leaf$days[leaf$lake == "Daulton Pond"])
plot(prop.rem.DP ~ leaf$days[leaf$lake == "Daulton Pond"])
plot(prop.rem.DP ~ leaf$days[leaf$lake == "Daulton Pond"])
plot(prop.rem.DP ~ leaf$days[leaf$lake == "Daulton Pond"], xlim = c(0, 30))
plot(log(prop.rem.DP) ~ leaf$days[leaf$lake == "Daulton Pond"], xlim = c(0, 30))
length(prop.rem.DP)
plot(log(prop.rem.CP) ~ leaf$days[leaf$lake == "Campus Pond"])
plot(log(prop.rem.CP) ~ leaf$days[leaf$lake == "Campus Pond"], xlim = c(0, 30))
plot(log(prop.rem.DP) ~ leaf$days[leaf$lake == "Daulton Pond"], xlim = c(0, 30))
points(log(prop.rem.CP) ~ leaf$days[leaf$lake == "Campus Pond"], col = 3)
points(log(prop.rem.LPP) ~ leaf$days[leaf$lake == "Lancer Park Pond"], col = 4)
plot(mean(log(prop.rem.DP)) ~ mean(leaf$days[leaf$lake == "Daulton Pond"]), xlim = c(0, 30))
plot(mean(log(prop.rem.DP)) ~ leaf$days[leaf$lake == "Daulton Pond"], xlim = c(0, 30))
plot(log(prop.rem.DP) ~ leaf$days[leaf$lake == "Daulton Pond"], xlim = c(0, 30))
points(log(prop.rem.CP) ~ leaf$days[leaf$lake == "Campus Pond"], col = 3)
points(log(prop.rem.LPP) ~ leaf$days[leaf$lake == "Lancer Park Pond"], col = 4)
q()
n
plot(CPOM.AFDM ~ julian, data = survey)
plot(CPOM.AFDM ~ julian, data = survey, subset = lake == "WL")
plot(location.cpom.aov)
str(survey)
plot(depth ~ location, data = survey)
plot(depth ~ as.factor(location), data = survey)
plot(depth ~ as.factor(location), data = survey)
plot(depth ~ as.factor(location), data = survey, col = c(4, 5))
hist(CPOM.AFDM, data = survey, col = c(2, 3, 4))
hist(survey$CPOM.AFDM, col = c(2, 3, 4))
hist(survey$CPOM.AFDM, col = c(2, 3, 4), breaks = 20)
fivenum(survey$CPOM.AFDM)
    dev.off()
q()
n
str(leaf)
plot(prop.rem.LPP ~ julian, data = leaf, subset = lake == "Lancer Park Pond")
length(prop.rem.LPP)
length(leaf$julian[leaf$lake == "Lancer Park Pond"])
plot(prop.rem.LPP ~ julian, data = leaf, subset = lake == "Lancer Park Pond")
plot(prop.rem.LPP ~ leaf$julian[leaf$lake == "Lancer Park Pond"])
plot(prop.rem.LPP ~ days, data = leaf)
length(leaf$days)
plot(prop.rem.LPP ~ days, data = leaf, subset = lake == "Lancer Park Pond")
plot(prop.rem.LPP ~ leaf$days[leaf$lake == "Lancer Park Pond"])
plot(prop.rem.LPP ~ leaf$days[leaf$lake == "Lancer Park Pond"])
summary(DP.k)
summary(CP.k)
summary(LPP.k)
q()
n
ls()
str(leaf_calc)
rm(list = ls())
ls()
ls()
summary(DP.k)
q()
y
str(leaf)
prop.rem.DP
ls()
rm(list = ls())
prop.rem.DP
T0.DP
leaf$AFDM
leaf$AFDM[leaf$lake == "DP"]
str(leaf)
leaf$AFDM[leaf$lake == "Daulton Pond"]
T0.DP
leaf[leaf$lake == "Daulton Pond"]
leaf[leaf$lake == "Daulton Pond",]
leaf$AFDM[leaf$lake == "Daulton Pond"]
leaf[25]
leaf[25,]
temp <- leaf$AFDM[leaf$lake == "Daulton Pond"]
temp[25,]
temp
temp <- leaf[leaf$lake == "Daulton Pond",]
temp[25,]
rm(temp)
leaf$AFDM[leaf$lake == "Daulton Pond"]
prop.rem.DP
temp <- leaf[leaf$lake == "Daulton Pond",]
temp[17,]
T0.DP
rm(list = ls())
rm(list = ls())
prop.rem.DP
str(leaf)
prop.rem.DP[-c(17, 25)]
leaf$days[leaf$lake == "Daulton Pond"]
leaf$sample[leaf$lake == "Daulton Pond"]
leaf$sample[leaf$lake == "Daulton Pond" & -c(17, 25)]
junk <- leaf$sample[leaf$lake == "Daulton Pond"]
junk
junk[-c(17, 25)]
prop.rem.DP[prop.rem.DP > 0]
prop.rem.DP
prop.rem.CP
prop.rem.CP
prop.rem.CP[prop.rem.CP > 0]
prop.rem.LPP[prop.rem.LPP > 0]
q()
n
